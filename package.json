{
    "name": "blog",
    "scripts": {
        "dev": "nuxt",
        "build": "nuxt build",
        "generate": "nuxt generate",
        "start": "nuxt start",
        "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
    },
    "dependencies": {
        "aos": "^2.3.4",
        "nuxt": "^2.15.8"
    },
    "devDependencies": {
        "@nuxt/postcss8": "^1.1.3",
        "autoprefixer": "^10.4.7",
        "postcss": "^8.4.14",
        "push-dir": "^0.4.1",
        "tailwindcss": "^3.0.24"
    }
}
